node_modules/
lib/
dist/
coverage/
*.log
.turbo/
.vite/
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo

# Runtime state (session bindings, weixin credentials/cursor when a file
# provider is configured). Never commit.
data/

# Local spike scratch (M0/M1 verification tooling)
.spike/
